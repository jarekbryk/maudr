% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignReactions.R
\name{assignReactions}
\alias{assignReactions}
\title{Assign reaction conditions to students}
\usage{
assignReactions(
  student_file = NULL,
  enzyme_file = NULL,
  project_path = ".",
  seed = 1234,
  verbose = TRUE
)
}
\arguments{
\item{student_file}{Optional path to Excel file containing student list
with columns \code{student_no}, \code{first_name}, and \code{surname}.}

\item{enzyme_file}{Optional path to Excel file containing enzyme
parameters (e.g. \code{reaction_parameters.xlsx}).}

\item{project_path}{Path to the top-level project folder created by
\code{\link[=initialiseProject]{initialiseProject()}}. Default is current working directory.}

\item{seed}{Random seed for reproducibility (default = 1234).}

\item{verbose}{Logical; if TRUE (default), prints progress messages.}
}
\value{
Invisibly returns a list with:
\describe{
\item{timestamp}{Timestamp string identifying the output folder.}
\item{metadata}{A tibble with all student–reaction assignments.}
}
}
\description{
Randomly assigns each student one substrate and one inhibition type
(excluding "no_inhibition"), then creates a paired condition for
"no_inhibition". Each student therefore has two rows:
one inhibited and one uninhibited reaction. The function writes
a metadata Excel file to the timestamped \code{assignments_output} folder.
}
\details{
If no input files are provided, demo datasets bundled with the package
are used automatically.
}
\examples{
\dontrun{
proj <- initialiseProject()
assignReactions(project_path = proj, seed = 4321)
}
}
