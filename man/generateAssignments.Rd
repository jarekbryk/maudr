% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateAssignments.R
\name{generateAssignments}
\alias{generateAssignments}
\title{Generate assignment Excel files for each student}
\usage{
generateAssignments(
  run_timestamp,
  project_path = ".",
  verbose = TRUE,
  cuv_vol = 0.003,
  eps = 6220,
  enz_vol = 0.1,
  substr_conc = c(0, 10, 20, 40, 80, 160),
  time_vec = c(0.17, 0.33, 0.5, 0.66, 0.83, 1),
  use_jitter = TRUE,
  jitter_sd = 0.02
)
}
\arguments{
\item{run_timestamp}{Timestamp string (YYYY-MM-DD_HHMM) from \code{\link[=assignReactions]{assignReactions()}}.}

\item{project_path}{Path to the top-level project folder (default ".").}

\item{verbose}{Logical; show progress messages (default TRUE).}

\item{cuv_vol}{Cuvette volume in litres (default 0.003).}

\item{eps}{Extinction coefficient (default 6220).}

\item{enz_vol}{Enzyme volume in millilitres (default 0.1).}

\item{substr_conc}{Vector of substrate concentrations in mM.}

\item{time_vec}{Vector of reaction time points in minutes.}

\item{use_jitter}{Logical; add random noise to absorbances (default TRUE).}

\item{jitter_sd}{Numeric; SD of multiplicative noise if \code{use_jitter = TRUE}
(default 0.02, i.e. ±2\% variability).}
}
\value{
Invisibly returns TRUE after writing all student assignment files
and updating metadata.
}
\description{
Reads the setup metadata (from \code{\link[=assignReactions]{assignReactions()}}) and produces per-student
Excel files with simulated absorbance vs. time data. Each student receives
both inhibited and uninhibited conditions in one sheet. The true inhibition
type remains in the instructor metadata.
}
\details{
Random measurement noise ("jitter") is optionally applied per student in a
reproducible way based on the run seed and student ID.
}
